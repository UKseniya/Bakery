package kz.epam.commands.user;

import kz.epam.commands.Command;

import javax.servlet.http.HttpServletRequest;

public class ReviewOrders implements Command {
    @Override
    public String execute(HttpServletRequest request) {
        return null;
    }
}
